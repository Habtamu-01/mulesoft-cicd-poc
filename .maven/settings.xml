<?xml version="1.0"?>
<settings>
<pluginGroups>
    <pluginGroup>org.mule.tools</pluginGroup>
</pluginGroups>

  <servers>
    <!-- connected app's required scopes: https://help.salesforce.com/s/articleView?id=001119708&type=1 -->
    <server>
      <id>Repository</id> <!-- make sure this matches the serverId property in pom.xml -->
      <username>~~Client~~</username>
      <password>${env.CONNECTED_APP_CLIENT_ID}~?~${env.CONNECTED_APP_CLIENT_SECRET}</password> <!-- these come from the build.yml and your github secrets -->
    </server>
<server>
      <id>anypoint-exchange-v3</id> <!-- make sure this matches the serverId property in pom.xml -->
      <username>~~Client~~</username>
      <password>${env.CONNECTED_APP_CLIENT_ID}~?~${env.CONNECTED_APP_CLIENT_SECRET}</password> <!-- these come from the build.yml and your github secrets -->
    </server>

  </servers>

<profiles>

    <profile>

        <id>mulesoft</id>

        <repositories>

            <repository>
                <id>mulesoft-releases</id>
                <name>MuleSoft Releases</name>
                <url>https://repository.mulesoft.org/releases/</url>
                <releases>
                    <enabled>true</enabled>
                </releases>
                <snapshots>
                    <enabled>false</enabled>
                </snapshots>
            </repository>

            <repository>
                <id>anypoint-exchange-v3</id>
                <name>Anypoint Exchange</name>
                <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
                <releases>
                    <enabled>true</enabled>
                </releases>
            </repository>

        </repositories>

        <pluginRepositories>

            <pluginRepository>
                <id>mulesoft-releases</id>
                <name>MuleSoft Releases</name>
                <url>https://repository.mulesoft.org/releases/</url>
                <releases>
                    <enabled>true</enabled>
                </releases>
                <snapshots>
                    <enabled>false</enabled>
                </snapshots>
            </pluginRepository>

        </pluginRepositories>

    </profile>

</profiles>

<activeProfiles>
    <activeProfile>mulesoft</activeProfile>
</activeProfiles>


</settings>